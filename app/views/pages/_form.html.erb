<%= form_with model: [ book, page ], id: "page-editor" do |form| %>
  <%= form.text_area :body, class: "input page__editor margin-block-half", contenteditable: true %>
  <%= form.submit "Save", hidden: true %>
<% end %>
