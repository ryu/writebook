<%= form_with model: [ book, page ], id: "page-editor" do |form| %>
  <%= form.text_field :title, required: true, autofocus: true, class: "input full-width" %>
  <%= form.text_area :body, class: "input editor margin-block-half" %>
  <%= form.submit "Save", hidden: true %>
<% end %>
