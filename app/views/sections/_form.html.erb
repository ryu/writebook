<%= form_with model: [ book, section ], id: "section-editor" do |form| %>
  <%= form.text_field :title, required: true, autofocus: true, class: "input full-width"  %>
  <%= form.submit "Save", hidden: true %>
<% end %>
