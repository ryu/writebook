<% content_for(:title) { page_title(@section, @book) } %>
<% @layout_class = "page" %>

<% content_for :header do %>
  <%= render "leaves/header", book: @book, leaf: @leaf %>
<% end %>

<div class="page--section">
  <h1><%= @section.title %></h1>
</div>

<% content_for :footer do %>
  <%= render "leaves/navigation", leaf: @leaf %>
<% end %>
